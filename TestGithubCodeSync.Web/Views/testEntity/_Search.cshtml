@using TestGithubCodeSync.Web.HtmlExtensions
@using TestGithubCodeSync.Web.Models
<!--add customized code between this region-->
<!--add customized code between this region-->

@model SearchModel
<div class="search-container">
    @using (Html.BeginForm("Search", "testEntity", FormMethod.Post, new {data_ajax = "true", data_ajax_method = "POST", data_ajax_mode = "replace", data_ajax_update = "#testEntity_DataList", name = "testEntity_Search", id = "testEntity_Search"}))
    {
        @Html.AntiForgeryToken()
        @Html.TextBoxFor(m => m.Name, new {@class = "form-control", placeholder ="Name"})
        @Html.HiddenFor(m => m.Pager.PageSize)

        <input class="m_btn" type="submit" value="Search"/>
    }
</div>

